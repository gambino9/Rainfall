#include <stdio.h>
#include <string.h>

void p()
{
    int ret_flush = fflush(file_stream) // flushes the output buffer of a stream
    
    gets()

    if (2952790016 & EAX)
    {
        printf()
        -Exit(0)
    }
    exit()

    puts()
    strdup()

    return;
}

int main(int argc, char ** argv)
{
    p()
    return 0;
}