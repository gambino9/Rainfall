#include <stdio.h>  
#include <stdlib.h>  
#include <string.h>  

int main(int argc, char **argv)
{
    int ret = atoi(argv[1]);
    char **dup_str;

    if (423 == ret)
    {
        dup_str = strdup(ret);

        // get group user
        int gid = getegid()
        // get real user ID of calling process
        geteuid

        setresgid

        setresuid

        execve

        fwrite

        return
    }
    // if not equal :

}